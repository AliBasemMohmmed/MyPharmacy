@{
    Layout = null;
    ViewBag.Title = "MyPharmacy";
}

<!DOCTYPE html>
<html lang="ar">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* تعديلات على الأسلوب */
        .container {
            @* background-color: #d4edda;
            /* لون الخلفية الأخضر */
            padding: 20px;
            /* تباعد الحواف */
            border: 1px solid #c3e6cb;
            /* لون الحدود */
            border-radius: 10px;
            /* انحناء الزوايا */ *@
            margin-top: 50px;
            /* تباعد من الأعلى */
        }

        @* .title-container {
            border: 2px solid #FFFFFF;
            /* لون الحدود أبيض */
            padding: 20px;
            /* هامش داخلي */
            background-color: #fafdfa;
            /* لون الخلفية أخضر */
        } *@

        body {
            background-image: url("../lib/img/aaa.jpg");
            background-size: cover;
          
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="title-container" style="  margin-bottom: 70px;">
            <!-- إضافة div للإطار الأبيض -->
            <h1 class="text-center mb-4" style="color: gold; text-shadow: 2px 2px 4px #000000;">صيدليتي</h1>

        </div>
        <div class="row">
            @* <div class="col-md-6 mb-4">
            <h2 class="mb-4">الوصفة الطبية</h2>
            <form>
            <div class="form-group">
            <textarea class="form-control" rows="5" placeholder="أدخل الوصفة الطبية هنا"></textarea>
            </div>
            </form>
            </div> *@
            <div class="col-md-6 mx-auto my-auto text-center">
                <h2 class="mb-4">تسجيل الدخول</h2>
                <form>
                    <div class="form-group">
                        <input type="text" class="form-control" dir="rtl" placeholder="اسم المستخدم">
                    </div>
                    <div class="form-group">
                        <input type="password" class="form-control" dir="rtl" placeholder="كلمة المرور">
                    </div>
                    @* <div class="form-group">
                        <select class="form-control" dir="rtl">
                            <option value="doctor">طبيب</option>
                            <option value="pharmacist">صيدلاني</option>
                            <option value="patient">مريض</option>
                            <option value="admin">مشرف</option>
                        </select>
                    </div> *@
                    <a asp-action="Doctor" class="btn btn-primary">تسجيل الدخول</a>
                    <!-- <button type="submit" class="btn btn-primary" formaction="RsvpForm">تسجيل الدخول</button> -->
                </form>
                @* <div class="mt-3 text-center">
                    <p>لا تملك حسابًا؟ <a asp-action="regester" style="color: gold; text-shadow: 2px 2px 4px #000000;">إنشاء حساب جديد</a></p>
                </div> *@
            </div>

        </div>
    </div>
</body>

</html>
